import React from "react";

type Props = {};

export const Button = (props: Props) => {
  return (
    <button className='btn btn-primary border-secondary border-2'>
      Click Me
    </button>
  );
};
